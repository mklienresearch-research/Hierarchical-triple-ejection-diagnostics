[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hierarchical-triple-ejection"
version = "0.1.0.dev0"
description = "Causal finite-time prediction of hierarchical-triple ejection and binary-single encounter outcomes"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "rebound>=4.0",
  "scikit-learn>=1.3",
  "xgboost>=2.0",
  "pandas>=2.0",
  "lifelines>=0.28",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "ruff>=0.6",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py310"
